package com.kamil.regex;

import jdk.nashorn.internal.runtime.regexp.joni.Regex;

public class Main {

    public static void main(String[] args) {
        com.kamil.regex.Regex regex = new com.kamil.regex.Regex();
        regex.replace();

    }
}
